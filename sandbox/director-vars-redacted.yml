---
opsman-configuration:
  vsphere:
    vcenter:
      datacenter: ((vcenter_DataCenter))
      datastore: ((vcenter_DataStore))
      url: ((vcenter_host_url))
      username: ((vcenter_login_username)) 
      password: ((vcenter_login_password))
      cluster_name: ((vcenter_cluster))
      resource_pool:  ((vcenter_resource_pool))
    infra_network:
      cidr: ((infra_network_cidr))
      reserved_range: ((infra_network_reserved_range))
      gateway:((infra_network_gateway)) 
      dns: ((infra_network_dns)) 
      vm_network: ((infra_network_vm_network_name)) 
      dns: 75.75.75.75,8.8.8.8
      vm_network: VM network
